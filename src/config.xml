<?xml version="1.0" encoding="UTF-8"?>
<beans init-method="init" id-method="contId">
  <Config id="config">
    <value set-as="logMappings">true</value>
    <value set-as="useWidgetId">true</value>
  </Config>

  <DimensionRestoreDatabase id="dimensionRestoreDatabase" />

  <!-- Tool map for MainController, and ToolList for addNode dialog. All the tools are defined here -->
  <import resource="src/tools.xml" />
  <!-- Main window -->
  <import resource="src/gui/main/main.xml" />
  <!-- Add node (tool selection dialog) -->
  <import resource="src/gui/addNode/addNode.xml" />
  <import resource="src/gui/about/about.xml" />
</beans>
